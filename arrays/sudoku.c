#include <stdio.h>

void randomizeSudoku(int board[][]);

int main(void) {
    int sudokuBoard[9][9] = {0};

}

void randomizeSudoku(int board[][]) {
    int i, j;
    for (i = 0; i < 9; i++) {
        for (j = 0; j < 9; i++) {
            if (board[i][j] != )
        }
    }
}